# Root requirements — used by Streamlit Community Cloud.
# NOTE: streamlit is intentionally NOT listed here. Streamlit Cloud preinstalls
# it; pinning it forces the resolver to uninstall/reinstall Streamlit's whole
# dependency tree, which is what caused "Error during processing dependencies".
# We only declare what the base image lacks. (Docker uses app/requirements.txt,
# which DOES include streamlit because the slim Python image has none.)
pandas>=2.2.3,<3
psycopg2-binary>=2.9.10
